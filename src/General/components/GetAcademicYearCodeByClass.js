
     export const  GetAcademicYearCodeByClass = async(C)=>{ 
      
      return "ola"
        /*  const  CL = C*1; 
         
        if(CL === "#"){setData(null);
        }else{
          const response = await axios.get(DATA_URL[9]+`${CL}`);   
          setData(response.data[0].ed_class_year);
        } 

        if(CL === "#") return "#";
        return data !== null ? data : null; */
     }
